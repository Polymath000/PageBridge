import 'package:flutter/material.dart';

class TokenViewBody extends StatelessWidget {
  const TokenViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Token View'));
  }
}
